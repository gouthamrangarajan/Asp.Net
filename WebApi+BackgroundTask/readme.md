> a simple web api + worker template example,
```bash
# webapi + background task hosted on same instance,
# data gets added by a background task every 5 seconds, 
# refresh the api screen to check it out
